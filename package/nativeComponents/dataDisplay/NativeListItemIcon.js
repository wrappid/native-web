import React from "react";
import { SCListItemIcon } from "../../styledComponents/dataDisplay/SCListItemIcon";

export default function NativeListItemIcon(props) {
  return <SCListItemIcon {...props} />;
}
